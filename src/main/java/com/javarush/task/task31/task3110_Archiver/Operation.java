package com.javarush.task.task31.task3110_Archiver;

/*
Создай enum Operation.
Он должен содержать следующие значения:
CREATE, ADD, REMOVE, EXTRACT, CONTENT, EXIT.
 */
public enum Operation {
    CREATE,
    ADD,
    REMOVE,
    EXTRACT,
    CONTENT,
    EXIT
}
